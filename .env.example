# Copy to .env (or just export it) to enable the live LLM steps with Gemini.
# Get a free key at https://aistudio.google.com/apikey
# Without a key, Demand Radar still runs end to end in heuristic/offline mode.

GEMINI_API_KEY=AIza...
